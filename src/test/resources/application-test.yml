logging:
  level:
    es.upm.miw: DEBUG

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/iwvgdevopsdb
    username: postgres
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
